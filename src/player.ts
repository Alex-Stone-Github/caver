class Player {
    private position: Vector;
    private _size: Vector;
    private velocity: Vector;
    public Player(position: Vector, size: Vector) {
        this.position = position;
        this._size = size;
    }
}
